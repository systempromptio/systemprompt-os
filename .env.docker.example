# SystemPrompt OS Docker Configuration Example
# Copy this file to .env and customize for your deployment

# Server Configuration
PORT=3000
NODE_ENV=production

# Base URL - Set this to your public URL when using Cloudflare or other reverse proxy
# This is used for OAuth callbacks and absolute URLs
BASE_URL=http://localhost:3000
# BASE_URL=https://your-domain.com

# OAuth Domain - Used for OAuth2 server metadata
OAUTH_DOMAIN=https://democontainer.systemprompt.io

# Paths (these are set correctly for Docker, don't change unless you modify volumes)
STATE_PATH=/data/state
PROJECTS_PATH=/data/projects
DATABASE_FILE=/data/state/systemprompt.db
CONFIG_PATH=/data/config
JWT_KEY_PATH=/data/state/auth/keys

# Workspace Configuration
WORKSPACE_PATH=./workspace
FILE_ROOT=/workspace
PROJECT_ROOT=/workspace
GIT_AVAILABLE=true

# Logging
LOG_LEVEL=info

# OAuth Providers (optional - for authentication)
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Get these from https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Cloudflare Tunnel (optional - for public access without port forwarding)
# Get a tunnel token from: https://one.dash.cloudflare.com/
ENABLE_OAUTH_TUNNEL=false
CLOUDFLARE_TUNNEL_TOKEN=

# MCP Configuration
MCP_AUTH_REQUIRED=true

# Docker-specific settings (used by docker-compose.yml)
# Leave these as-is unless you know what you're doing
DOCKER_TAG=latest