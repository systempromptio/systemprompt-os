# SystemPrompt OS Configuration

# Project root directory for task execution (REQUIRED)
# This is the directory where agents will execute tasks
PROJECT_ROOT=/path/to/your/project

# Server port (optional, defaults to 3000)
# This is the external port that the Docker container exposes
PORT=3000

# Tool paths and availability (these will be auto-detected by the setup/start scripts)
# GEMINI_PATH=/path/to/gemini
# SHELL_PATH=/bin/bash
# GEMINI_AVAILABLE=false

# JWT secret for authentication (optional, defaults to dev secret)
JWT_SECRET=your-secret-here

# Push token for mobile app notifications (optional)
# Get your push token from the SystemPrompt mobile app: Settings > Developer
# PUSH_TOKEN=your-push-token-here

# Docker Compose project name (optional, defaults to systemprompt-os)
# This ensures consistent container naming across different environments
COMPOSE_PROJECT_NAME=systemprompt-os

# ============================================
# OAuth Configuration
# ============================================

# OAuth Providers
# Google OAuth - Get from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth - Get from https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth Tunnel Configuration (Choose one option)
# --------------------------------------------

# Option 1: Automatic temporary tunnel (RECOMMENDED FOR DEVELOPMENT)
# Creates a temporary public URL automatically using Cloudflare
ENABLE_OAUTH_TUNNEL=true

# Option 2: Use permanent domain (RECOMMENDED FOR PRODUCTION)
# Set your own domain that points to this server
# OAUTH_DOMAIN=https://oauth.yourdomain.com

# Option 3: Use Cloudflare tunnel with token (for persistent tunnels)
# Create a tunnel: cloudflared tunnel create systemprompt
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token

# Advanced OAuth Configuration (usually auto-configured)
# BASE_URL=http://localhost:3000
# OAUTH_REDIRECT_URI=http://localhost:3000/oauth2/callback