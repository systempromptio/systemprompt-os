#!/usr/bin/env node

/**
 * Main CLI entry point for systemprompt-os
 */

import('../build/cli/src/index.js').catch(err => {
  console.error('Failed to start systemprompt:', err);
  process.exit(1);
});