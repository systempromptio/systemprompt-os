name: executors
type: service
version: 1.0.0
description: Placeholder executor module for future task and process management
author: SystemPrompt OS
dependencies:
  - logger
  - database
enabled: true
singleton: true

cli:
  commands:
    - name: executors:list
      description: List all executors (placeholder command)
      options:
        - name: format
          alias: f
          type: string
          description: Output format (table, json)
          default: table
          choices: [table, json]

exports:
  - services
  - types